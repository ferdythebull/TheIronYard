= simple_form_for [@forum, @topic] do |f|
  - if params[:forum]
    %input#topic_forum_id{name: "topic[forum_id]", type: "hidden", value: params[:forum]}/
  %p
    = f.input :name
  / %p
  /   %textarea{cols: "80", name: "post[content]", rows: "20"}= @post.content
  %p= f.submit "Create"