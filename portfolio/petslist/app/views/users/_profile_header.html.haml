%section.page
  #profile_header.group
    / = image_tag current_user.image.url(:big)
    %article
    %profile_header_info
      .name_and_attributes
        %ul
          %li#username= @user.name
          %li
            #{@user.gender}