%ol.breadcrumb
  %li
    =link_to "Home", root_path
  %li.active
    #{@user.name}
.col-md-12
  .row
    .col-md-6
      %h1 #{@user.name}
      = link_to 'Add Image', new_user_image_path(@user)
      .pull-right= render 'admin_buttons'
    .col-md-6
  .col-md-9
    .row
      = render 'info'
  .col-md-3
    = render 'profile_header'
    / = link_to "Profile", account_settings_user_path(current_user)