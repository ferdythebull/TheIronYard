.row
  .col-md-8
    = link_to "Account Settings", account_settings_user_path(@user)
    >> Overview
.row
  .col-md-2
  .col-md-6.list-group
    .list-group-item
      %h4.list-group-item-heading
        = image_tag current_user.avatar.url(:thumb)
        #{current_user.first_name} #{current_user.last_name}'s Account Settings
    .list-group-item
      = link_to "Profile", edit_user_path(@user)
      %br
      Edit your name, public profile information, and your Member Search preference.
    .list-group-item
      = link_to "Password", edit_user_path(@user)
      %br
      Change the password for your account.
    .list-group-item
      = link_to "Email / Notifications", edit_user_path(@user)
      %br
      Manage your email accounts, notifications, and newsletter subscriptions.
    .list-group-item
      = link_to "Locations", edit_user_path(@user)
      %br
      Add, edit, or remove your saved locations.
    .list-group-item
      = link_to "Friends", edit_user_path(@user)
      %br
      Remove friends and track friend invitations you have sent.
  .col-md-2